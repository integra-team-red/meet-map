CREATE TABLE tags
(
    id       BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    name     VARCHAR(255),
    category VARCHAR(255),
    CONSTRAINT pk_tag PRIMARY KEY (id)
);

ALTER TABLE tags
    ADD CONSTRAINT uc_f33b5f5ffb61e6451b3b32d1f UNIQUE (name);
